const state = {
	authenticated: false
};

const getters = {
	isAuthenticated(state) {
		return state.authenticated;
	}
};

const mutations = {};

const actions = {};

export default {
	state,
	getters,
	mutations,
	actions
};